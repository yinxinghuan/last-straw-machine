# Today's Last Straw

Drop your own avatar through a real Plinko board, land on one of three familiar office disasters, then watch a ten-second live-action reversal.

```bash
npm install
npm run dev
npm run build
```

The game uses Vite `base: './'` and can be hosted at any deployment subpath. Production media manifests and rejected visual evidence are retained under `_production/`.
